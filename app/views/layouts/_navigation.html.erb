<a class="item">
  <%= link_to 'Chatroom', root_path, class: "item" %>
</a>
<% if !logged_in? %>
  <a class="item">
    <%= link_to 'Register', login_path, class: 'item'%>
  </a>
  <a class="item">
    <%= link_to "Log in", login_path, class: "item"%>
  </a>
<% end %>
<div class="right menu">
  <div class="ui dropdown item">
    Account <i class="dropdown icon"></i>
    <div class="menu">
      <%= link_to "Log in", login_path, class: "item" if !logged_in? %>
      <%= link_to "Register", login_path, class: "item" if !logged_in? %>
      <%= link_to "Log out", logout_path, method: :delete, class: "item" if logged_in? %>
    </div>
  </div>
  <div class="item">
      <div class="ui primary button">Sign Up</div>
  </div>
</div>